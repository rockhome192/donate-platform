node_modules/
.pnpm-store/

# builds
.next/
out/
dist/
build/
*.tsbuildinfo

# env — keep the example, ignore the real ones
.env
.env.*
!.env.example

# test / coverage
coverage/
.vitest/

# editors & OS
.vscode/
.idea/
# third-party agent skills, vendored locally — not part of the product
.claude/
.DS_Store
Thumbs.db

# misc
*.log
*.zip
.vercel

# local OBS recordings — a demo clip is a portfolio artifact, not source, and a
# 6MB mp4 in a repo that may go public is a download nobody asked for
test-donate-obs/
*.mp4
